"use client";import { useEffect, useState } from "react";import type { LetterSummary } from "@/types";import { LetterCard } from "@/components/cards/LetterCard";export default function DashboardPage() {  const [letters, setLetters] = useState<LetterSummary[]>([]);  const [loading, setLoading] = useState(true);  useEffect(() => {    fetch("/api/history")      .then((r) => r.json())      .then((data) => setLetters(data.letters ?? []))      .catch(console.error)      .finally(() => setLoading(false));  }, []);  const stats = [    { label: "Letters sent", value: letters.length },    { label: "Total views", value: letters.reduce((s, l) => s + l.views, 0) },    { label: "Total reactions", value: letters.reduce((s, l) => s + l.reactions, 0) },  ];  return (    <div className="mx-auto max-w-4xl px-6 py-16">      <h1 className="font-hand text-4xl text-ink sm:text-5xl">Your letters</h1>      <p className="mt-2 text-sm text-muted">Everything you've written, in one place.</p>      <div className="mt-8 grid grid-cols-1 gap-4 sm:grid-cols-3">        {stats.map((stat) => (          <div key={stat.label} className="rounded-card border border-hairline bg-paper p-5">            <p className="font-hand text-3xl text-ink">{stat.value}</p>            <p className="mt-1 text-xs uppercase tracking-wide text-muted">{stat.label}</p>          </div>        ))}      </div>      <div className="mt-12">        <div className="flex items-center justify-between">          <h2 className="text-sm font-medium uppercase tracking-wide text-muted">History</h2>          <a href="/create" className="text-sm font-medium text-ink underline underline-offset-4 hover:no-underline">            + Write a new letter          </a>        </div>        <div className="mt-4 space-y-4">          {loading ? (            <p className="text-sm text-muted">Loading...</p>          ) : letters.length === 0 ? (            <p className="text-sm text-muted">You haven't written a letter yet.</p>          ) : (            letters.map((letter) => <LetterCard key={letter.id} letter={letter} />)          )}        </div>      </div>    </div>  );}